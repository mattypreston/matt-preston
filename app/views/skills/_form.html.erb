<%= form_for @skill, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :skill_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :skill_name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :skill_level, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :skill_level, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                skills_path, :class => 'btn' %>
  </div>
<% end %>
